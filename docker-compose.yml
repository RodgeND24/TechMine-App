version: '3.8'

services:

  backend:
    build:
      context: ./server
    container_name: techmine_fastapi_server
    ports:
      - "8800:8800"
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
    container_name: techmine_frontend
    ports:
      - "3000:3000"
    volumes:
      - ./techmine/build/web:/usr/share/nginx/html:ro
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - app-network
  
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
      # - "443:443" if HTTPS
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
    depends_on:
      - backend
      - frontend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
